#include "tools/Concepts.h"

// this file likely does not need to exist, but for now it's the only cpp in the whole project
// without it cmake is unable to detirmine the linker language for the project